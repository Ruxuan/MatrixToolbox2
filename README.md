# MatrixToolbox2
Matrix calculator version 2

```sh
$ make
javac -classpath out/ -d out/ src/*
java -classpath out/ Main
-------------------------------------------------
Main menu:
<a> Add matrix
<r> Remove matrix
<s> Select matrix
<v> View matrix list
<e> Exit
Input:
```
